% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{tl_datasets_owned_all}
\alias{tl_datasets_owned_all}
\title{Fetch every owned dataset, paging transparently}
\usage{
tl_datasets_owned_all(client, page_size = 50, total_limit = NULL)
}
\arguments{
\item{client}{A `topolab_client`.}

\item{page_size}{Datasets per request, 1-200 (default 50).}

\item{total_limit}{Max datasets overall (NULL = all).}
}
\value{
A list of owned-dataset entries, in server order. The reported total
  is attached as `attr(x, "total")`.
}
\description{
Pages `tl_datasets_owned()` on `offset` until the reported `total` is
reached, stopping early on a short or empty page.
}
